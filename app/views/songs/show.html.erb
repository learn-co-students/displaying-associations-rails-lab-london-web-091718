<h2><%= @song.title %></h2>

<h3><%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %></h3>

<%= link_to 'All artists', artists_path %><br>
<%= link_to 'All songs', songs_path %>
