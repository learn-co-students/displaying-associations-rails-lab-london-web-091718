<h1>All Songs</h1>

<ul>
  <% @songs.each do |s| %>
    <li>
      <a href= "<%=song_path(s)%>" >
        <%= s.artist.name %>
        -
        <%= s.title %>
      </a>
    </li>
  <% end %>
</ul>

<%= link_to 'All artists', artists_path %>
